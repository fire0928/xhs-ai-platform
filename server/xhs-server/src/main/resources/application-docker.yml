spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:xhs_db}?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USER:xhs_user}
    password: ${DB_PASS:Xhs@2024!user}
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASS:XhsRedis@2024}

minio:
  endpoint: ${MINIO_ENDPOINT:http://minio:9000}
  access-key: ${MINIO_ACCESS_KEY:xhs_admin}
  secret-key: ${MINIO_SECRET_KEY:XhsMinio@2024}

jwt:
  secret: ${JWT_SECRET:XhsJwtSecretKey2024Secure256bit!!}
